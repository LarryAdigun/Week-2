#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main (void)
{
    string name = "Larry";

    for( int i = 0, length = strlen(name); i < length ; i++) {
        //printf("%c\n", name[i]);
        //printf("letter: \t%i length: \t%i\n", i, length);
       // printf("%c\n", name[letter]);
        printf("%c", name[letter]);


    }

}
int main (void)
{
    string name = "Larry";

    for( int i = 0, length = strlen(name); i < length ; i++) {
        //printf("%c\n", name[i]);
        //printf("letter: \t%i length: \t%i\n", i, length);
       // printf("%c\n", name[letter]);
        printf("%c", name[letter]);


    }
    int main (void)
{
    string name = "Larry";

    for( int i = 0, length = strlen(name); i < length ; i++) {
        //printf("%c\n", name[i]);
        //printf("letter: \t%i length: \t%i\n", i, length);
       // printf("%c\n", name[letter]);
        printf("%c", name[letter]);


    }

}

int main (void)
{
    string name = "Larry";

    for( int i = 0, length = strlen(name); i < length ; i++) {
        //printf("%c\n", name[i]);
        //printf("letter: \t%i length: \t%i\n", i, length);
       // printf("%c\n", name[letter]);
        printf("%c", name[letter]);


    }

}

}